# Apply this setting on every host PC that receives LiDAR UDP data.
# It is a host kernel setting, not a setting stored inside the LiDAR.
# Linux may report SO_RCVBUF as 67108864 bytes because it doubles the
# requested value internally for socket bookkeeping; that is normal.
net.core.rmem_max = 33554432
